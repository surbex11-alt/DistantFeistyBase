tasks.register("assembleDebug") {
    doLast {
        println "React Web Build Ready"
    }
}

tasks.register("lint") {
    doLast {
        println "Linting React Web codebase clean"
    }
}

tasks.register("check") {
    doLast {
        println "Check passed"
    }
}

